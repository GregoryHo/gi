import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function subagentsExtension(_pi: ExtensionAPI): void {
	// M1 tool registration is implemented in the active milestone.
}
