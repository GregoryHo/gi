# Goal mode versioned planning

Use this directory for future post-MVP version planning.

Each active version folder should contain at least:

```text
versions/<semver>/
  index.md
  milestones.md
  log.md
```

Root-level milestone files cover the initial MVP planning era only.
