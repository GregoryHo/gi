# Agent workers extension log

Append important planning decisions, milestone starts/completions, verification evidence, and handoff notes here.

## 2026-07-06

- Started active v0.4.0 versioned planning area under `versions/0.4.0/` on branch `feature/pi-native-agent-worker-adapter`. M1 scopes Phase 4 pi-native sub-agent behavior as a `pi-sdk` worker adapter inside `pi-extension-agent-workers`, preserving the existing generic worker runtime rather than creating a separate package.

## 2026-07-07

- Implemented and documented the main v0.4.0 M1 `pi-sdk` adapter slices: async adapter runtime support, pi SDK child-session adapter, public adapter wiring, confirmation-gated real-adapter classification, conservative child tool scopes, and async terminal-path tests. README, CHANGELOG, orchestration recipes, and v0.4.0 docs now describe behavior, non-goals, safety boundaries, and usage.
- Completed v0.4.0 M1 verification. Passed: `npm test --workspace @gregho/pi-extension-agent-workers` (127 tests), `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
- Manual interactive `pi-sdk` smoke passed after explicit model-use invocation: `/worker-run --adapter pi-sdk --yes Reply with OK only.` produced `run_1783409485446_445c9626`; the local run index records `completed`, `exit_zero`, `exitCode: 0`, `finalText: OK`, `usage.source: reported`, elapsed `2350ms`, and the log contains `[stdout] pi-sdk child session completed`.
- Completed v0.4.0 local release prep. Bumped `@gregho/pi-extension-agent-workers` package metadata and lockfile entry to `0.4.0`, promoted the changelog to `0.4.0 - 2026-07-10`, updated README/docs/release policy, sealed `versions/0.4.0/`, and left npm publish/git tag undone by design. Verification passed after the bump: `npm test --workspace @gregho/pi-extension-agent-workers` (127 tests), `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers` (reported `@gregho/pi-extension-agent-workers@0.4.0` and included `src/adapters/pi-sdk.ts`), `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.

## 2026-05-25

- Created initial docs and package scaffold on branch `feature/pi-extension-agent-workers`.
- Established package/docs name `pi-extension-agent-workers` following the repo 1:1 naming convention.
- Scoped M1/M2 to generic worker supervision and event/usage parsing. Jira delegation remains deferred.
- Verified scaffold with `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, and `npm run typecheck --workspaces --if-present`.
- Refined proposed M1/M2 plans: M1 uses a safe demo adapter and command-only UI; M2 requires a worker CLI event-format research checkpoint before parser implementation.
- Started M1 Worker runner console: status moved to `In progress`. Implementation scope is safe demo adapter, command-first UI, one worker process, local logs, status/log/cancel commands, and `usage.source = "unknown"`.
- Completed M1 Worker runner console. Implemented safe `demo` adapter, `/agent-workers`, `/worker-run`, `/worker-status`, `/worker-log`, `/worker-kill`, one-running-worker enforcement, local log capture, and M1 unknown usage reporting. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`. Non-interactive load smoke `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"` exited successfully.
- M1 manual testing confirmed status/log/safe-adapter behavior. Follow-up patch added capped demo `--duration-ms` for reliable cancellation testing and clearer `/worker-kill` output when a worker already completed.
- Started M2 Worker event and usage parsing: status moved to `In progress`. First step is the required worker CLI event-format research checkpoint; parser implementation should wait until observed versions, commands, sanitized event shapes, and fallback assumptions are recorded.
- Began M2 research checkpoint in `research.md`: recorded local Claude Code and Codex CLI paths, versions, JSON-related help flags, proposed harmless stream commands, and fallback assumptions. No model/task execution had been run yet at initial checkpoint creation.
- With approval, ran short machine-readable sample tasks for Claude Code and Codex CLI. Raw output stayed under `/tmp/agent-workers-m2-research/` and was not committed. `research.md` now records sanitized event shapes and parser assumptions. Claude emitted stream events but exited non-zero due to the deliberately low budget cap; Codex emitted JSONL successfully.
- Completed M2 Worker event and usage parsing as parser/normalization support, not default real worker CLI execution. Implemented shared `WorkerEvent`/expanded `WorkerUsage`, Claude Code `stream-json` parser, Codex CLI JSONL parser, worker manager event application, compact status summaries, reported usage display, unknown fallback for missing usage, and malformed-line fallback. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`. Non-interactive load smoke `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"` exited successfully.
- Roadmap adjusted after manual M2 review: external real worker execution is generic infrastructure, not domain delegation. Added proposed M3 Real worker CLI adapters for explicit Claude/Codex adapters, and moved domain delegation to deferred M4.
- Started M3 Real worker CLI adapters: status moved to `In progress`. Implementation scope is explicit `claude-code` and `codex-cli` adapters using M2 parsers, safe argv construction with `shell: false`, one running worker, CLI availability errors, and explicit confirmation before real worker execution.
- Completed M3 Real worker CLI adapters. Implemented explicit Claude Code and Codex CLI adapters, confirmation gate, CLI availability validation, safe argv construction with `shell: false`, parser wiring, and reported usage/status output. Manual smoke in pi confirmed `claude-code` and `codex-cli` each completed `Reply with OK only.` with `exitCode: 0`, `usage.source: reported`, activity summaries, and `final: OK`; raw logs stayed under `~/.pi/agent/agent-workers/runs/<id>/`. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`. Non-interactive load smoke `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"` exited successfully.
- Roadmap adjusted after product discussion: `agent-workers` should stay domain-independent and expose a reusable invocation contract. Replaced M4 domain delegation with proposed M4 Worker invocation API and profiles; moved Jira/worktree/tool/event-bus integrations to deferred M5 wrappers/examples.
- Started M4 Worker invocation API and profiles: status moved to `In progress`. Implementation scope is a generic WorkerRequest/Profile/Result contract, built-in profiles, profile-backed `/worker-run`, and a small reusable service API without domain-specific commands.
- Completed M4 Worker invocation API and profiles. Implemented exported `WorkerRequest`/`WorkerProfile`/`WorkerResult` contracts, `AgentWorkerService`, built-in `planner`/`reviewer` profiles, profile-backed `/worker-run`, and compact result shaping without raw event payloads. Manual smoke in pi confirmed `/worker-run --profile planner Reply with OK only.` ran via `adapter: claude-code` and completed with `exitCode: 0`, `usage.source: reported`, activity summary, and `final: OK`; raw logs stayed under `~/.pi/agent/agent-workers/runs/<id>/`. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`. Non-interactive load smoke `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"` exited successfully.
- Roadmap adjusted after integration discussion: M5 should expose `agent-workers` through LLM-callable tools rather than adding domain-specific Jira/worktree code. Proposed tools: `agent_worker_start`, `agent_worker_status`, `agent_worker_cancel`, and `agent_worker_list_profiles`; Jira + worker scenarios should be composed by LLM tool orchestration across loaded extensions.
- Started M5 LLM tool facade: status moved to `In progress`. Implementation scope is generic `agent_worker_*` tools over the shared M4 service, compact outputs, shared command/tool state, and no domain-specific code.
- Completed M5 LLM tool facade. Implemented `agent_worker_start`, `agent_worker_status`, `agent_worker_cancel`, and `agent_worker_list_profiles` over the shared M4 service. Tool outputs are compact summaries without raw event payloads, and commands/tools share the same service instance. Manual smoke confirmed `agent_worker_list_profiles` returned `planner`/`reviewer`, `agent_worker_start` with `adapter: demo` started `run_1779696304986_3a2f89f8`, and `agent_worker_status` reported it completed. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`. Non-interactive load smoke `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"` exited successfully.
- Roadmap adjusted for M6: prefer orchestration recipes and examples before adding runtime wrappers. Proposed M6 documents Jira + worker and standalone worker recipes using M5 tools; future runtime integration surfaces move to deferred M7 unless recipes reveal concrete friction.
- Started M6 Orchestration examples and recipes: status moved to `In progress`. Implementation scope is README/docs recipes only, covering Jira + workers, standalone workers, safe demo smoke, output interpretation, and safety notes.
- Completed M6 Orchestration examples and recipes. Added `orchestration-recipes.md` and README guidance for safe demo smoke, standalone planning/implementation, and Jira-focused issue planning/implementation through LLM tool composition. Documented output interpretation and safety notes. No runtime surfaces, Jira-specific code, worktree automation, event bus integration, or model calls were added. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
- Roadmap adjusted after Jira worker smoke: the main friction was workers running in the wrong repository, not prompt shape. Proposed M7 Worker workspace picker and preflight using native pi UI (`ctx.ui.select`, `ctx.ui.input`, `ctx.ui.confirm`) instead of custom TUI; future integration surfaces move to deferred M8.
- Started M7 Worker workspace picker and preflight: status moved to `In progress`. Implementation scope is native workspace commands, `--cwd` support, shared selected workspace default, cwd validation/preflight warnings, and effective cwd in worker confirmations/output.
- Completed M7 Worker workspace picker and preflight. Implemented `src/workspaces.ts`, `/worker-workspace`, `/worker-workspace-pick` with native `ctx.ui.select()`/`ctx.ui.input()`, `/worker-run --cwd`, selected workspace fallback, cwd validation/git-root detection, advisory mismatch warnings, and effective cwd in tool summaries and real-worker confirmations. Added tests for command parsing, native picker command behavior, workspace discovery/resolution/validation, preflight warnings, and tool cwd output/confirmation. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`, `pi -e ./packages/pi-extension-agent-workers --no-session -p "/worker-workspace"`, and demo cwd smoke via `/worker-run --cwd $(pwd) --adapter demo hello from cwd preflight`.
- Started M7.1 Per-run workspace assignment after manual acceptance feedback. Sticky selected workspace fallback is confusing for central orchestration projects; target semantics are now `effective cwd = explicit cwd || current pi cwd`, with `/worker-run --pick-cwd` as a one-run native picker.
- Completed M7.1 Per-run workspace assignment. Removed sticky selected workspace state/fallback, added `/worker-run --pick-cwd` for one-run native picker selection, changed `/worker-workspace` to current cwd/git-root hints only, and changed `/worker-workspace-pick` to print a picked path without setting defaults. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`, `pi -e ./packages/pi-extension-agent-workers --no-session -p "/worker-workspace"`, and demo cwd smoke via `/worker-run --cwd $(pwd) --adapter demo hello from per-run cwd`.
- Roadmap adjusted for initial release: proposed M8 v0.1.0 release preparation to bump `@gregho/pi-extension-agent-workers` from `0.0.0` to `0.1.0`, promote changelog/release docs, run verification, and avoid committing unrelated `package-lock.json` changes. Future integration surfaces move to deferred M9.
- Started M8 v0.1.0 release preparation: status moved to `In progress`. Scope is package version bump, changelog/README/docs release status, verification, and no unrelated lockfile commit.
- Completed M8 v0.1.0 release preparation. Bumped `@gregho/pi-extension-agent-workers` package metadata to `0.1.0`, promoted changelog entries into `0.1.0 - 2026-05-25`, updated README/docs status, and left existing unrelated `package-lock.json` changes uncommitted. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers` (reported `@gregho/pi-extension-agent-workers@0.1.0` and `gregho-pi-extension-agent-workers-0.1.0.tgz`), `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
- Started v0.2.0 planning cleanup. Superseded earlier deferred Jira/runtime-integration wording: Jira/tool composition is considered covered by the completed M5/M6 generic tool and recipe model. Updated current planning direction toward broader reliable delegated-worker loops: timeout/wait support, local run artifact indexing, safer implementation profiles, and v0.2.0 release prep.
- Drafted v0.2.0 milestone set after product discussion: M9 worker wait/timeout and rich summaries, M10 run artifact index/history, M11 expanded safe profiles with `implementer` and `verifier` (preferred over `qa`), M12 persistent widget cards, M13 bounded six-worker dispatch with workspace safety, and M14 v0.2.0 release preparation.
- Started M9 Worker wait/timeout and rich run summaries: status moved to `In progress`. Implementation scope is enforced `timeoutMs`, `agent_worker_wait`, `/worker-wait`, distinct timed-out status, and richer compact summaries without raw logs.
- Completed M9 Worker wait/timeout and rich run summaries. Implemented enforced `timeoutMs`, distinct `timed_out` status and status reasons, `/worker-run --timeout-ms`, `agent_worker_wait`, `/worker-wait`, caller wait-limit behavior that does not cancel runs, and richer compact summaries without raw logs. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
- Started M10 Run artifact index and recent history: status moved to `In progress`. Implementation scope is compact local run metadata indexing, `agent_worker_list_runs`, `/worker-history`, and historical-only informational summaries after restart without process reattachment.
- Completed M10 Run artifact index and recent history. Implemented compact local `runs-index.json` metadata under the artifact root, start/terminal run index updates, `agent_worker_list_runs`, `/worker-history`, bounded recent-history listing, and historical-only summaries marked `controllable: false` after restart. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`. Manual smoke passed: `/worker-run --adapter demo --duration-ms 3000 history smoke` appeared in `/worker-history` as completed with `controllable: true`; after restart, the same run appeared as `completed — historical` with `controllable: false`.
- Started M11 Expanded safe profiles: implementer and verifier. Implementation scope is adding built-in `implementer` and `verifier` profiles with safety metadata, confirmation/read-only semantics, expanded profile listing, and tests for profile resolution and command/tool exposure.
- Completed M11 Expanded safe profiles. Implemented built-in `implementer` and `verifier` profiles, expanded `WorkerProfile` safety metadata, minimal-diff/no-unrelated-refactor/verification prompts, read-only verifier guidance, expanded `/worker-run --profile ...` help, and metadata-rich `agent_worker_list_profiles` results. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`. Manual smoke passed: `agent_worker_list_profiles` listed `planner`, `reviewer`, `implementer`, and `verifier`; `/worker-run --profile verifier --yes ...` started a `claude-code` `mode: review` run, performed read-only git status checks, completed with `exitCode: 0`, and reported `PASS` with evidence/caveats. A follow-up usage-text fix added the expanded `--profile planner|reviewer|implementer|verifier` options to `/worker-run` usage; package tests and typecheck passed after the fix.
- Started M12 Persistent worker widget cards. Implementation scope is a safe interactive `agent-workers` widget using pi widget APIs, showing up to 6 compact current/recent worker cards, updating on worker interactions/state changes, and remaining a no-op in non-UI mode.
- Completed M12 Persistent worker widget cards. Implemented an interactive `agent-workers` widget with up to 6 compact current/recent cards, status icons/text, profile/mode, adapter, cwd basename, elapsed time, task preview, activity/final/status summaries, width-safe truncation, run-change refreshes, session-start rendering, session-shutdown clearing, and non-UI no-op behavior. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`. Manual smoke passed with `/agent-workers`, `/worker-status`, and `/worker-run --adapter demo --duration-ms 5000 widget smoke`; truncation behavior was accepted for M12.
- Started M13 Bounded six-worker dispatch. Implementation scope is replacing the one-running-worker limit with a hard maximum of 6 active workers, conservative read/write classification, same-workspace write-capable collision blocking, distinct-workspace allowance, multi-run status/widget clarity, and cancel-all safety.
- Completed M13 Bounded six-worker dispatch. Implemented a hard cap of 6 active workers, slot metadata, read-only/write-capable safety metadata, same-workspace write collision blocking, distinct-workspace write allowance, conservative direct-real-adapter classification, multi-run status/widget visibility, and multi-worker cancel-all behavior. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`. Manual smoke passed after reloading the updated extension: two demo workers ran concurrently with `slot: 1` and `slot: 2`, `readOnly: true`, `canModifyWorkspace: false`, expected `workspaceKey`, `/agent-workers` listed both running workers, and `/worker-status` later showed both completed with `statusReason: exit_zero` and `exitCode: 0`. The old one-worker-limit message was observed only before the runtime reload.
- Started M14 v0.2.0 release preparation. Scope is package version bump to `0.2.0`, dated changelog, README/docs current-release updates, release verification, pack dry-run version check, and no unrelated lockfile changes, with no npm publish or git tag unless explicitly requested.
- Completed M14 v0.2.0 release preparation. Bumped `@gregho/pi-extension-agent-workers` package metadata to `0.2.0`, promoted changelog entries into `0.2.0 - 2026-05-26`, updated README/docs status, updated the release-policy package version table, and left `package-lock.json` unchanged because no source-loaded workspace lockfile update was needed. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers` (reported `@gregho/pi-extension-agent-workers@0.2.0` and `gregho-pi-extension-agent-workers-0.2.0.tgz`), `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`. No npm publish or git tag was created.
- Sealed v0.2.0 docs after comparing the Jira board versioned-docs convention. Root-level M0-M14 planning docs are now marked historical, `index.md` has no active planning version, `archive.md` indexes completed root milestone plans, and future product iterations should use `versions/<semver>/`.
- Started active v0.3.0 versioned planning area under `versions/0.3.0/` with index, milestone tracker, and log. Root `index.md` now points to v0.3.0 as the active planning version.
- Completed v0.3.0 local release preparation. Bumped `@gregho/pi-extension-agent-workers` package metadata to `0.3.0`, promoted `CHANGELOG.md` to `0.3.0 - 2026-05-27`, sealed `versions/0.3.0/` docs, updated root docs/archive/release policy, and left npm publish/git tag undone by design. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers` (reported `@gregho/pi-extension-agent-workers@0.3.0` and `gregho-pi-extension-agent-workers-0.3.0.tgz`), `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
- Planned and completed v0.3.1 patch release preparation. Removed the temporary `/worker-ui-poc` runtime command and PoC-only source after the accepted compact card widget direction shipped as the default worker widget, fixed stale `/agent-workers` `M1 commands` wording, normalized orphaned historical active runs as stale failed history for history/widget displays, bumped package metadata to `0.3.1`, updated README/changelog/docs/release policy, and left npm publish/git tag undone by design. Verification evidence is recorded in `versions/0.3.1/m1-remove-worker-ui-poc-command.md`.
