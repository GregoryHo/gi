# Agent workers v0.3.0 log

Append v0.3.0 planning decisions, milestone starts/completions, verification evidence, and handoff notes here.

## 2026-05-26

- Started v0.3.0 planning on branch `feature/pi-extension-agent-workers-v0.3.0` after sealing root-level v0.2.0 docs.
- Agreed that milestone numbering restarts per version folder; logs and handoffs should use full names such as `v0.3.0 M2`.
- Agreed v0.3.0 theme: workspace-aware history/config, original task preview cleanup, and Widget/TUI capability PoC before finalizing worker UI direction.
- Drafted v0.3.0 milestone set: M1 version planning setup, M2 workspace-scoped history, M3 original task preview cleanup, M4 workspace-scoped config, M5 Widget/TUI capability PoC, M6 worker UI direction implementation, M7 custom profiles from config, M8 release prep.
- Completed v0.3.0 M1 Version planning setup. Added M1-M8 milestone plan docs with SPEC/AC/status tracking, marked M1 Done, and linked plans from the v0.3.0 tracker/index. No runtime changes.
- Started v0.3.0 M2 Workspace-scoped history. Scope is run identity/history by git root or cwd, current-workspace default history views, all-workspaces fallback views, and backward-compatible old index entries.
- Completed v0.3.0 M2 Workspace-scoped history. Implemented git-root/cwd workspace scope metadata on runs/results/history, scoped history defaults for `/worker-history`, `agent_worker_list_runs`, and widget history, all-workspaces fallback via `/worker-history --all` and `scope: "all"`, and backward-compatible old index handling. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
- Started v0.3.0 M3 Original task preview cleanup. Scope is preserving original user delegated task separately from profile/system-prompt-expanded worker prompts, and updating status/history/tool/widget summaries to prefer the original task preview.
- Completed v0.3.0 M3 Original task preview cleanup. New service-started runs preserve the user's original delegated task preview separately from the resolved profile/system-prompt-expanded worker task. Status/history/widget surfaces for new runs now use the original task preview; old historical entries may still show their stored `System prompt: ...` preview. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
- Started v0.3.0 M4 Workspace-scoped config. Scope is local ignored workspace config, `/worker-config` read/update surfaces, safe defaults for worker start/history/widget preferences, and no silent weakening of real-worker confirmation.
- Completed v0.3.0 M4 Workspace-scoped config. Implemented local per-workspace config storage, `/worker-config` read/update surfaces, safe defaults for profile/adapter/timeout/history/widget preferences, command/tool confirmation planning through config-aware request resolution, and widget/history preference application. Config cannot silently disable real-worker confirmation. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
- Started v0.3.0 M5 Widget/TUI capability PoC. Scope is explicit, removable PoC commands to exercise widget component/factory, overlay custom UI, and footer/status APIs, plus a decision note documenting stable public pi UI capabilities and recommendation.
- Implemented v0.3.0 M5 PoC runtime surface and provisional decision note. Added explicit `/worker-ui-poc [widget|footer|cockpit|all|clear]` to exercise component-backed widget, custom footer/status, and overlay custom UI APIs without changing default behavior. Decision note: `m5-ui-capability-decision.md`. Automated verification pending full baseline; manual interactive TUI smoke is still required before marking M5 Done.
- Automated verification for M5 PoC passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`. M5 remains In progress pending manual interactive `/worker-ui-poc` smoke.
- Completed v0.3.0 M5 Widget/TUI capability PoC after manual interactive smoke. User confirmed `/worker-ui-poc all` renders widget/footer/overlay surfaces and observed it is visually cluttered with six demo worker runs. Decision: do not pursue simultaneous all-surfaces UI; M6 should implement compact default widget + status badge, with any overlay cockpit remaining explicit/on-demand. Decision note updated: `m5-ui-capability-decision.md`.
- Reopened v0.3.0 M5 after user feedback: current PoC proved APIs render, but did not fully answer the core widget-height/available-width problem. Next M5 work must target component widget width behavior, widget height/truncation limits, and whether custom overlay is required for a useful worker cockpit.
- Added targeted M5 follow-up PoC modes after user asked to validate widget height/width alternatives: `/worker-ui-poc wide-widget` for width-aware multi-column component widget rendering, and `/worker-ui-poc sidepanel` for right-anchored overlay sidepanel behavior with percentage width, minWidth, maxHeight, and responsive visibility.
- Refined M5 PoC ergonomics after manual screenshot review: `/worker-ui-poc` non-clear modes now clear previous PoC widget/status/footer surfaces before installing the requested surface, and output is mode-specific. This makes `wide-widget` and `sidepanel` easier to evaluate independently; `all` remains an explicit clutter/stress test. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers` and `npm run typecheck --workspace @gregho/pi-extension-agent-workers`.
- Added `/worker-ui-poc card-widget` after user requested card/item-style basics. The PoC renders compact worker cards with adapter, profile, run id, and duration, and refreshes current workspace history every 5 seconds using the widget component factory lifecycle. Verification: focused command tests and package typecheck passed.
- Refined `/worker-ui-poc card-widget` based on user feedback that the original widget structure was good but used width/space poorly. The card PoC now keeps original-style bordered cards while limiting fields to adapter/profile/run id/duration and rendering two bordered columns on wide terminals. Verification passed: focused commands test and package typecheck.
- Refined `/worker-ui-poc card-widget` after manual review: removed the visual middle divider between wide columns, capped card width smaller, added task and reason lines, and kept all card fields truncated to card width. Verification passed: package tests and typecheck.
- Completed v0.3.0 M5 after final manual acceptance of `/worker-ui-poc card-widget`. Final decision: M6 should promote an original-style compact card widget as the persistent worker UI direction, with smaller cards, two-column wide rendering, no middle divider, 5-second refresh, and truncated fields for adapter/profile/run id/duration/task/reason. Overlay sidepanel remains an explicit detailed cockpit candidate, not the default persistent UI.
- Started v0.3.0 M6 Worker UI direction implementation. Scope: promote the accepted M5 compact card-widget direction into the default persistent `agent-workers` widget, preserving current-workspace defaults and adding interval refresh for elapsed/duration freshness.
- Implemented M6 default widget direction from M5: the production `agent-workers` widget now uses compact original-style cards with adapter/profile/duration/task/reason, narrower card width, two-card wide layout without a middle divider, truncation, current-workspace scoping, and 5-second refresh. Automated package test/typecheck passed; manual interactive widget smoke pending before M6 completion.
- Fixed M6 default widget to use a component/factory instead of pre-rendered string lines. This lets pi pass the actual widget width into `render(width)`, so the compact two-column card layout works in the real widget rather than only in pure renderer tests. Verification passed: package tests and typecheck.
- Refined M6 widget cards after user feedback: restored the worker slot in card titles and added concrete local started time (`HH:mm:ss`) alongside duration, so the widget shows both how long a worker ran and when it started.
- Completed v0.3.0 M6 after manual default-widget smoke passed. Final default widget behavior: component-factory width-aware compact original-style cards, slots in titles, local started time, duration, adapter/profile/task/reason truncation, current-workspace scoping, and 5s refresh. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
- Started v0.3.0 M7 Custom profiles from config. Initial scope: support local workspace config `profiles` arrays with validation, merge built-ins plus custom profiles for listing and resolution, reject built-in name overrides, and preserve safety metadata for confirmation/concurrency behavior. No committed project profile files or secrets.
- Completed v0.3.0 M7 Custom profiles from config. Implemented workspace config `profiles`, strict validation, built-in override rejection, real-adapter confirmation enforcement, service profile listing/resolution, and tool profile listing with workspace custom profiles. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
- Started v0.3.0 M8 release prep. Scope: bump package to 0.3.0, promote changelog, seal v0.3.0 docs/root pointers, run final verification. No npm publish or git tag unless explicitly requested.
- Completed v0.3.0 M8 release prep. Bumped package metadata to `0.3.0`, promoted changelog to `0.3.0 - 2026-05-27`, updated README/root docs/release policy, sealed v0.3.0 docs, and left publish/tag undone by design. Verification passed: `npm test --workspace @gregho/pi-extension-agent-workers`, `npm run typecheck --workspace @gregho/pi-extension-agent-workers`, `npm run pack:dry-run --workspace @gregho/pi-extension-agent-workers` (reported `@gregho/pi-extension-agent-workers@0.3.0` and `gregho-pi-extension-agent-workers-0.3.0.tgz`), `npm run typecheck`, and `pi -e ./packages/pi-extension-agent-workers --no-session -p "/agent-workers"`.
