# pi-extension-subagents

## Status

- Package scaffolded on `feature/subagents-runtime-facade`.
- M1 foreground read-only facade is complete.

## Product boundary

Subagents provides delegation UX and agent definitions over Agent Workers protocol v1. It does not own worker execution, adapters, lifecycle, safety, history, artifacts, orchestration loops, or team coordination.

## Active milestone

See [M1 — Foreground read-only delegation facade](m1-foreground-read-only-facade.md).
