# Versioned planning

After the initial release is sealed, future version planning should live under `versions/<semver>/`.

Each active version folder should contain at least:

```text
versions/<semver>/
  index.md
  milestones.md
  log.md
```
